## What this changes

<!-- One or two sentences. Link the issue it closes, if any. -->

Closes #

## Why

<!-- The problem, not the diff. -->

## How it was verified

- [ ] `pytest` passes
- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] `mypy` passes
- [ ] If this touches the constraint engine: the hand-span guarantee tests still pass
- [ ] If this touches rendering: a sample video was generated and eyeballed
- [ ] If this fixes a bug: the new test was checked against the *old* code and failed
- [ ] If this regenerates a reference image: the commit message says why the
      picture changed

## Notes for the reviewer

<!-- Trade-offs, follow-ups, anything deliberately left out. -->
